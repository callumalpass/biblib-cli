{
  "name": "biblib-cli",
  "version": "0.2.0",
  "description": "CLI for retrieving bibliographic metadata via Zotero Translation Server and writing CSL-JSON YAML frontmatter",
  "type": "module",
  "bin": {
    "biblib": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "yaml": "^2.8.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
